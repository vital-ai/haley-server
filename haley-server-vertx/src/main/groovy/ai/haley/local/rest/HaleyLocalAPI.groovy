package ai.haley.local.rest

class HaleyLocalAPI {

	// status
	// used by browser to test if local server is present
	// perhaps return something specific to haley to ensure it's the local haley server 
	// and not something else with a "status" call
	// i.e. localhost/status might be some other server
	
	
	// local devices
	// list, register, unregister, status of device
	
	// plugins
	
	// session status with Haley
	
	// synchronous message exchange
	// to devices
	// to haley
	
	
	// authenticate
	
	
	
}
